# 第三方组件与数据声明（NOTICE）

本项目（Desktop Pet）主体代码以 MIT 许可发布（见 `LICENSE`）。
**插件代码**随主仓库一并发布；**插件数据、美术素材、逆向实现的风险**见下方各节。

## 设计与图标

- **Lucide 图标**（ISC License）：`core/icons.py` 内嵌的线条图标。
- **Ant Design 设计规范**（MIT）：全局 QSS 主题令牌（颜色 / 圆角 / 间距）。
- **WorkBuddy 视觉语言**：设置对话框外观仅作**取色参考**，未包含其任何代码或资源。

## 游戏联动

- **Minecraft 中文 Wiki 内容**（CC BY-NC-SA）：本地知识库数据（`wiki_data/mcwiki.db`
  与 curated 词条）属于 **wiki-local 插件**，**不随主仓库发布**；可由
  `plugins/wiki-local/wiki_build.py` 重建。
- **车万女仆（Touhou Little Maid）**：皮肤/模型资源参考自其开源仓库
  （MIT / CC BY-NC-SA，见其仓库许可）。本仓库不包含其资源文件。

## 美术素材 ⚠️ 不适用 MIT

### 角色形象：「大肥鱼」/ 女仆鲸鱼娘 —— **不是本项目原创**

该角色有明确的上游创作链，**版权属于原作者**：

| 环节 | 作者 | 时间 | 说明 |
|---|---|---|---|
| 原创 OC「溟月」 | **上善无形** | 2025-06 | 蓝色渐变长发、鲸类头鳍、鲸尾等基础设定 |
| 女仆鲸鱼娘（现流行形象） | **ZipZipPipe**（B 站） | 2026-04-25 | 在「溟月」基础上融入 DeepSeek 元素，改深蓝白色女仆装 |

据两位作者 2026-08-02 的公开声明，该形象以 **CC BY-NC-SA 4.0** 开放二次创作，即：

- **署名（BY）**：必须标注原作者；
- **非商业（NC）**：**不得用于商业目的**；
- **相同方式共享（SA）**：衍生作品须以相同协议发布。

因此：

- `assets/_static/idle.png`（大肥鱼待机帧，**随主仓库发布**）与完整动画帧
  （`assets/<动作>/`，见下）**都是该角色的衍生作品，按 CC BY-NC-SA 4.0 使用**，
  **不在本项目 MIT 许可的覆盖范围内**；
- **代码是 MIT，美术素材不是** —— 两套许可请分别遵守。
  尤其注意：**NC 条款意味着包含该形象的整套分发不得用于商业目的**；
- 想完全摆脱该约束：用 `deskpet-tools/` 从**自己的**素材制作角色包
  （`skins/<角色id>/`），替换掉默认形象即可。

> 若原作者对具体用途另有授权，以实际授权为准。

### 素材文件分布

- **完整动画帧**（`assets/<动作>/`，1663 帧）**不在本仓库**，发布在配套的
  **动画资源仓库**（`Multimodal-AI-Companion-Assets`）的 Release 附件里，
  **同样按 CC BY-NC-SA 4.0 使用**；
- 绿幕素材源（离线加工输入）不发布，可用 `deskpet-tools/` 从自备视频制作。

## 可选插件

> **插件代码随主仓库发布，可自由选用**；下表中标注"不随包"的**数据/素材**需使用者自建或自备。

- **deepseek-web 插件**（代码随仓库）：通过 `chat.deepseek.com` 的**非官方内部接口**访问 DeepSeek
  网页版。它**不是**官方 SDK、与 DeepSeek 官方无关；使用者需自行确认并遵守服务方的用户协议，
  账号与可用性风险自担；网页端改版可能导致失配。**推荐优先使用官方 API 或千问**。
- **wiki-local 插件**（代码随仓库，**数据不随包**）：其知识库数据派生自 Minecraft 中文 Wiki
  （CC BY-NC-SA），**不随主仓库分发**，需用 `plugins/wiki-local/wiki_build.py` 自行构建。
- **agentic-rag 插件**（代码随仓库，**数据不随包**）：`rag/` 下的检索 / 建库代码迁自本项目
  自研的 **RAG 方案（`mcwiki-agentic-rag`）**，同一作者、MIT；向量库 / BM25 索引 / 切分数据
  （约 345MB，含 Wiki 派生的 CC BY-NC-SA 内容）**不随主仓库分发**，需按插件 README 自行构建。
  依赖 `faiss` / `sentence-transformers`（含 torch）由使用者自行安装。
- **素材包 / 角色包**（不随包）：按各自许可单独发布。

## Python 依赖

各依赖遵循其自身许可（PySide6/LGPL、curl_cffi/MIT、numpy/BSD、playwright/Apache-2.0、
wasmtime/Apache-2.0 等），见 `requirements.txt` / `requirements-web.txt`。
